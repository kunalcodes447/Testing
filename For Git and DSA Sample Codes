Sample Code - 

Day 1 

Problem 1
Problem Statement - 
You are given an array of integers.
Your task is to find and return the smallest element present in the array.
Solution - 

int findSmallest(int[] arr){
int min = arr[];
for (int i=1; i<arr.length; i++){
     if(arr[i]<min){
        min = arr[i];
    }
  }
return min;
}

* Final Time Complexity - O(n)
* Final Space Complexity - O(1) (constant space)


Problem 2
Problem Statement - 
Find the sum of all elements in an array
Solution - 

int[] arr = {2,4,6,8};
int sum = 0;
for (int i=0; i<arr.length; i++){
     sum = sum + arr[i];
}
System.out.println(sum);

* Final Time Complexity - O(n)
* Final Space Complexity - O(1) (constant space)


Problem 3
Problem Statement - 
Find the maximum element in an array
Solution - 

int[] arr = {574,68,88,845,0,6,8,9,3,545,74};
int max = arr[0];
for (int i=0; i<arr.length; i++){
     if(arr[i] > max){
       max = arr[i];
     }
}
System.out.println(max);

* Final Time Complexity - O(n)
* Final Space Complexity - O(1) (constant space)



Problem 4
Problem Statement -
Given an integer array arr, determine whether the array is sorted in non-decreasing order
Solution -

public class checkSorted {

     public static boolean isSorted (int[] arr){
         for (int i=0; i<arr.length-1; i++){
               if(arr[i] > arr[i+1]){
                    return false;
                  }
               }
            return true;
          }

     public static void main(String[] args){
          int[] arr = {1,2,3,4,5,6};
          System.out.println(isSorted(arr));
     }
}


* Final Time Complexity - O(n)
* Final Space Complexity - O(1) (constant space)



Problem 4
Problem Statement -
Given an integer array, find the second largest element
Solution -

public class SecondLargest {

    public static int findSecondLargest(int[] arr) {
        if (arr.length < 2) {
            return -1;
        }

        int largest;
        int secondLargest;

        if (arr[0] > arr[1]) {
            largest = arr[0];
            secondLargest = arr[1];
        } else if (arr[1] > arr[0]) {
            largest = arr[1];
            secondLargest = arr[0];
        } else {
            largest = arr[0];
            secondLargest = -1;  // temporary variable
        }

        for (int i = 2; i < arr.length; i++) {

            if (arr[i] > largest) {
                secondLargest = largest;
                largest = arr[i];
            } 
            else if (arr[i] > secondLargest && arr[i] < largest) {
                secondLargest = arr[i];
            }
        }

        return secondLargest;
    }

    public static void main(String[] args) {
        int[] arr = {10, 5, 20, 8};
        System.out.println(findSecondLargest(arr));
    }
}

* Final Time Complexity - O(n)
* Final Space Complexity - O(1) (constant space)




Problem 4
Problem Statement -
Given an array, reverse it without using extra arrays
Solution -

public class ReverseArray {

    public static void reverse(int[] arr) {

        int left = 0;
        int right = arr.length - 1;

        while (left < right) {

            int temp = arr[left];
            arr[left] = arr[right];
            arr[right] = temp;

            left++;
            right--;
        }
    }

    public static void main(String[] args) {

        int[] arr = {1, 2, 3, 4, 5};

        reverse(arr);

        for (int x : arr) {
            System.out.print(x + " ");
        }
    }
}


* Final Time Complexity - O(n)
* Final Space Complexity - O(1) (constant space)




Problem 5
Problem Statement -
Given an array, determine whether it is sorted in non-decreasing order.
Solution -

public class CheckSorted {

    public static boolean isSorted(int[] arr) {

        for (int i = 0; i < arr.length - 1; i++) {

            if (arr[i] > arr[i + 1]) {
                return false;
            }
        }

        return true;
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)




Problem 6
Problem Statement -
Given an array and a number target, count how many times it appears
Solution -

public class Frequency {

    public static int countFreq(int[] arr, int target) {

        int count = 0;

        for (int i = 0; i < arr.length; i++) {

            if (arr[i] == target) {
                count++;
            }
        }

        return count;
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)




Problem 7
Problem Statement -
Given an array, find the largest value
Solution -

public class LargestElement {

    public static int findLargest(int[] arr) {

        int largest = arr[0];

        for (int i = 1; i < arr.length; i++) {

            if (arr[i] > largest) {
                largest = arr[i];
            }
        }

        return largest;
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)





Problem 8
Problem Statement -
Given an array and a target value, return the index where it appears
Solution -

public class LinearSearch {

    public static int findIndex(int[] arr, int target) {

        for (int i = 0; i < arr.length; i++) {

            if (arr[i] == target) {
                return i;
            }
        }

        return -1;
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)






Problem 9
Problem Statement -
Given an array, count how many numbers are:
Even
Odd
Solution -

public class CountEvenOdd {

    public static void count(int[] arr) {

        int even = 0;
        int odd = 0;

        for (int i = 0; i < arr.length; i++) {

            if (arr[i] % 2 == 0) {
                even++;
            } else {
                odd++;
            }
        }

        System.out.println("Even = " + even);
        System.out.println("Odd = " + odd);
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)





Problem 10
Problem Statement -
Given an array, compute the total sum of its elements
Solution -

public class SumArray {

    public static int sum(int[] arr) {

        int total = 0;

        for (int i = 0; i < arr.length; i++) {
            total = total + arr[i];
        }

        return total;
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)





Problem 10
Problem Statement -
Given an array, count how many elements are negative
Solution -

public class CountNegative {

    public static int countNeg(int[] arr) {

        int count = 0;

        for (int i = 0; i < arr.length; i++) {

            if (arr[i] < 0) {
                count++;
            }
        }

        return count;
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)





Problem 11
Problem Statement -
Given an array, find the second largest element without using built-in sorting or functions
Solution -

public class SecondLargest {

    public static int findSecondLargest(int[] arr) {

        int largest = arr[0];
        int second = -1;

        for (int i = 1; i < arr.length; i++) {

            if (arr[i] > largest) {
                second = largest;
                largest = arr[i];
            }
            else if (arr[i] > second && arr[i] != largest) {
                second = arr[i];
            }
        }

        return second;
    }
}


Final Time Complexity - O(n)
Final Space Complexity - O(1) (constant space)




















